html
  head
   title Login
   meta(name='description', content='Home page')
   meta(charset='utf-8')
   meta(http-equiv='X-UA-Compatible', content='IE=edge')
   meta(name='viewport', content='width=device-width, initial-scale=1')
   link(rel='stylesheet', href='/public/style.css')
   link(rel="stylesheet", href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css")
  body
   div(class="container")
    h1(id="title") Login
    if success_msg != ''
     div(class="alert alert-success", role="alert") #{success_msg}
    else if error_msg != ''
     div(class="alert alert-warning", role="alert") #{error_msg}
    else if error != ''
     div(class="alert alert-warning", role="alert") #{error}
    form(action="/login", method="post", id="form")
     div
      label Username:
      input(type='text', name='username' id="username-input", required)
     div
      label Password:
      input(type="password", name="password", id="password-input", required)
     div
      input(type="submit", value="Log In", id="submit", class="btn btn-primary btn-large")
     a(href="/identity", id="forget-link") Forgot Password?
     p Don't have an account? 
     a(href="/register" class="register-link") Register here